import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#2092ec',
      main: '#0082ea',
      dark: '#0376d2',
      contrastText: '#fff',
    },
  },
});

export default theme;
