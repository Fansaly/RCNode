const NAME = 'RCNode';

const PREFIX = 'RCN_';
const AUTH = `${PREFIX}AUTH`;
const SETTINGS = `${PREFIX}SETTINGS`;

const AXIOS_CONFIG = {
  baseURL: 'https://cnodejs.org/api/v1',
  timeout: 0,
};

export { AUTH, AXIOS_CONFIG, NAME, SETTINGS };
